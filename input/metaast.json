{
  "funcs": [
    {
      "name": "getUser",
      "args": [
        { "name": "id", "type": "number" }
      ],
      "ret": {
        "type": "object",
        "properties": {
          "id": { "type": "number" },
          "profile": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "verified": { "type": "boolean" },
              "tags": { "type": "array", "elementType": { "type": "string" } }
            }
          }
        }
      },
      "docstring": "Retrieves a user profile with nested structures."
    },
    {
      "name": "deploy",
      "args": [
        { "name": "config", "type": "object", "properties": { "env": { "type": "string" } } }
      ],
      "ret": { "type": "boolean" },
      "docstring": "Deploys the application."
    },
    {
      "name": "validate",
      "args": [],
      "ret": { "type": "boolean" },
      "docstring": "Validates the deployment."
    }
  ],
  "references": [
    { "from": "deploy", "to": "validate", "type": "call" }
  ]
}
