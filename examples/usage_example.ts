/// <reference path="./generated/metacall.d.ts" />

// This file demonstrates how the generated stubs provide IntelliSense.
// Since the stubs are declared globally (ambient), sum should be available.

const result = sum(5, 10);
console.log(`Result: ${result}`);

// TypeScript will error if types are wrong:
// const bad = sum("5", 10); // Argument of type 'string' is not assignable to parameter of type 'number'.
